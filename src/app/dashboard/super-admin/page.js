export default function SuperAdminDashboard() {
  return (
    <div>
      <h1 style={{ fontSize: '2rem', fontWeight: 800, marginBottom: '1rem' }}>Control Room</h1>
      <p style={{ color: 'var(--text-secondary)' }}>Welcome, Super Admin. You have full access to all modules.</p>
    </div>
  );
}
